apiVersion: v1
kind: Service
metadata:
  name: access-url
spec:
  selector:
    app: access-url
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-url
spec:
  replicas: 1
  selector:
    matchLabels:
      app: access-url
  template:
    metadata:
      labels:
        app: access-url
    spec:
      containers:
      - name: access-url
        image: yourname/access_url
        ports:
        - containerPort: 8080
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: access-url.domain.local
spec:
  tls:
  - hosts:
    - access-url.domain.local
    secretName: domain
  rules:
  - host: access-url.domain.local
    http:
      paths:
      - path: /
        backend:
          serviceName: access-url
          servicePort: 8080
